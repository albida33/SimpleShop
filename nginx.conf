server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Serve any file that exists; return 404 otherwise
    location / {
        try_files $uri $uri/ =404;
    }

    # Don't expose the nginx version number in error pages or headers
    server_tokens off;
}
